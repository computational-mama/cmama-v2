  <p class="font-comp font-bold text-4xl">some work and texts below</p>

  {% for post in projlist %}
    <div class="m-5">
        <a class="font-cirrus italic text-2xl" href="{{ post.url | url }}">{{ post.data.title }}</a>
    </div> 
  {% endfor %}
  
